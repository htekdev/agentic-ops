name: Test Blocking Workflow
description: A test workflow to verify blocking mode
blocking: true

on:
  hooks:
    types: [preToolUse]

steps:
  - name: Echo test
    run: echo "Workflow executed successfully"
