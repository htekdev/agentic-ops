name: Test Non-Blocking Fail Workflow
description: A test workflow that fails but allows in non-blocking mode
blocking: false

on:
  hooks:
    types: [preToolUse]

steps:
  - name: Failing step
    run: exit 1
